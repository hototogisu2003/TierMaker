import TeamManager from "@/component/teamcustom/TeamManager";

export const metadata = {
  title: "TeamBuild - Memo",
};

export default function TeamBuildTeamPage() {
  return <TeamManager mode="memo" />;
}
